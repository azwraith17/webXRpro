(define-module (leo)
  #:use-module (model-viewer)
  #:use-module (artanis artanis))

(define-public (leo-allmighty rc)
  `(html (@ (lang "en"))
	 (head
	  (meta (@ (charset "utf-8")))
	  (title "Leo Almighty"))
	 (body
	  ,@(gen-model-viewer "leo.glb")
	  ;; (model-viewer (@ (ar)
	  ;;		   (alt "Leo Model")
	  ;;		   (src "./leo.glb")
	  ;;		   ;; (ios-src "./leo_texture.usdz")
	  ;;		   (style "width:480;height:720;")
	  ;;		   (shadow-intensity "1")
	  ;;		   (camera-controls)
	  ;;		   (touch-action "pan-y")
	  ;;		   (interaction-prompt "auto")
	  ;;		   (auto-rotate)
	  ;;		   (scale "1 1 1")
	  ;;		   (ar-modes "webxr scene-viewer quick-look")
	  ;;		   (magic-leap))
	  ;;		"")
	  ;; (script (@ (type "module")
	  ;;	     (src "./model-viewer.js"))
	  ;;	  "")
	  )))
